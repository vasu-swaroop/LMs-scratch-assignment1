


1. Did not delete the token pair from the token pair registry when I found the most frequent
2. After merging, I did not apply a recursive merge update on the token_pair
3. Was not updating the token list when creating new

LLM Used for
1. Adding logging/tqdm
2. Fixing typing inconsitency, name bugs
3. Taking idea about how to hash/tuple the token pair. Hashing of dataclass